language: python
python:
  - 2.7

notifications:
  email: false
  irc:
    channels:
      - "us.freenode.net#sosreport"
    on_success: change

before_install:
 # add for profile
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - sudo apt-get update -qq

install:
  - "pip install six nose nose-cov --use-mirrors"
  - "python setup.py install"
    # add for profile
  - sudo apt-get install -qq g++-4.8
  - export CXX="g++-4.8"
  - pip install nose-cprof
  - pip install properform

script:
  - nosetests -v --with-cover --cover-package=sos --cover-html --with-cprofile --cprofile-stats-erase --cprofile-stats-file=profile_sos_aa0a14db01_3
  - python -m properform Push 47.88.50.110:15554 asdf_sos_aa0a14db01_3 ./profile_sos_aa0a14db01_3
 #2017-08-12 11:17:35 modified at 0 #2017-08-12 11:17:40 modified at 1 #2017-08-12 11:17:44 modified at 2 #2017-08-12 11:17:49 modified at 3