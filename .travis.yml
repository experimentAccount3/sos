language: python
python:
  - 2.7

matrix:
  allow_failures:
    - python: 3.4

before_install:
 # add for profile
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - sudo apt-get update -qq


install:
  - "pip install six nose nose-cov --use-mirrors"
  - "python setup.py install"
  # add for profile
  - sudo apt-get install -qq g++-4.8
  - export CXX="g++-4.8"
  - pip install nose-cprof
  - pip install properform


script:
  - nosetests -v --with-cover --cover-package=sos --cover-html --with-cprofile --cprofile-stats-erase --cprofile-stats-file=profile_sos_241d240a25_3
  - python -m properform Push 47.88.50.110:15554 asdf_sos_241d240a25_3 ./profile_sos_241d240a25_3

 #2017-08-12 10:50:55 modified at 0 #2017-08-12 10:51:00 modified at 1 #2017-08-12 10:51:04 modified at 2 #2017-08-12 10:51:08 modified at 3