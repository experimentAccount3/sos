language: python
python:
  - 2.7

matrix:
  allow_failures:
    - python: 3.5
    - python: "nightly"

before_install:
 # add for profile
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - sudo apt-get update -qq


install:
  - "pip install six nose nose-cov pep8"
  - "python setup.py install"
    # add for profile
  - sudo apt-get install -qq g++-4.8
  - export CXX="g++-4.8"
  - pip install nose-cprof
  - pip install properform

script:
  - "pep8 sos"
  - nosetests -v --with-cover --cover-package=sos --cover-html --with-cprofile --cprofile-stats-erase --cprofile-stats-file=profile_sos_14fcddf627_1
  - python -m properform Push 47.88.50.110:15554 asdf_sos_14fcddf627_1 ./profile_sos_14fcddf627_1
 #2017-08-12 08:38:53 modified at 0 #2017-08-12 08:38:58 modified at 1